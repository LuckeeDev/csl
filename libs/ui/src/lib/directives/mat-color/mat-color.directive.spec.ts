import { MatColorDirective } from './mat-color.directive';

describe('MatColorDirective', () => {
  it('should create an instance', () => {
    const directive = new MatColorDirective();
    expect(directive).toBeTruthy();
  });
});
