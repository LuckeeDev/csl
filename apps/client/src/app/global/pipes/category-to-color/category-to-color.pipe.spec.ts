import { CategoryToColorPipe } from './category-to-color.pipe';

describe('CategoryToColorPipe', () => {
  it('create an instance', () => {
    const pipe = new CategoryToColorPipe();
    expect(pipe).toBeTruthy();
  });
});
