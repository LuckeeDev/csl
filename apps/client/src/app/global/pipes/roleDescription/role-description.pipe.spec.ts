import { RoleDescriptionPipe } from './role-description.pipe';

describe('RoleDescriptionPipe', () => {
  it('create an instance', () => {
    const pipe = new RoleDescriptionPipe();
    expect(pipe).toBeTruthy();
  });
});
