{
	"targets": {
		"version": {
			"executor": "@jscutlery/semver:version",
			"options": {
				"syncVersions": true,
				"noVerify": true,
				"rootChangelog": true,
				"commitMessageFormat": "release(csl): 🏫 release version ${version}"
			}
		}
	},
	"root": ".",
	"tags": []
}
