{
	"targets": {
		"version": {
			"executor": "@jscutlery/semver:version",
			"options": {
				"syncVersions": true,
				"push": true,
				"rootChangelog": true,
				"postTargets": ["workspace:github"]
			}
		},
		"github": {
			"executor": "@jscutlery/semver:github",
			"options": {
				"tag": "${tag}",
				"notes": "${notes}",
				"title": "Release ${tag}"
			}
		}
	},
	"root": ".",
	"tags": []
}
